import XCTest
@testable import OctoprintInfoSwift

final class OctoprintInfoSwiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OctoprintInfoSwift().text, "Hello, World!")
    }
}
